version: 2

snapshots:
  - name: passengers_snapshot
    relation: source('source', 'passengers')
    config:
      schema: snapshots
      unique_key: passenger_id
      strategy: timestamp
      updated_at: processed_at